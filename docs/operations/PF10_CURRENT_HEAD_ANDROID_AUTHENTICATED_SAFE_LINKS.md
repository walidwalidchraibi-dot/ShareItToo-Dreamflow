# PF10 current-head Android authenticated safe links

Status: **BOUNDED AUTHENTICATED SAFE-LINK PASS — FIXTURE LINKS, DEVICE MATRIX AND STAGE A HOLD / NO-GO**

Observed: 2026-08-23

PF10 verifies the read-only authenticated app-link safety subset on the
authorized Pixel and exact PF6 candidate. It does not use a credential vault,
enter credentials, log out, create an account or mutate account, booking,
Support, notification or Payment state.

## Source-bound route

The app-link diagnostic has a paired `--current-head` and
`--preserve-authenticated-session` route. Either flag without the other is
rejected, and the route accepts neither a candidate-directory nor vault
override. Before ADB interaction it revalidates the PF6 repository evidence,
owner-only archive and exact installed direct APK.

The reusable historical guest route remains unchanged. Current Android
keyguard fields are also recognized so the tool never attempts to enter or
bypass a lock code.

## Physical result

The already-authenticated read-only notification surface was present before
and after all probes. Between those observations, the exact verified HTTPS
route for a deliberately missing listing produced its bounded unavailable
surface, an identifier containing an encoded path separator was rejected back
to the ordinary start surface, and Android confirmed that a foreign web host
is not associated with the ShareItToo package.

UI hierarchies were transient, deleted after every read and never stored in
evidence. The repository record contains no notification content, account
identity, credentials, tokens, review account data, private path, network
identifier or raw device identifier. The app was returned to its launcher
surface with the session preserved.

## Remaining scope

PF10 does not prove authenticated fixture listing, booking or chat links,
Google Play delivery, the full functional/device matrix, a booking flow, real
push or pilot readiness. Those continue to require their own isolated fixture
or external evidence. No live/public/provider boundary changed and Stage A
remains `HOLD / NO-GO`.
