# WP05 — saved-detail descendant ownership

Exact implementation clean closure now PASS on
`952a388eab9fa6f61a85d5537af04f55df94c3d6`: full690s, secondAndroid44s,
byte-identical APKs/entries, unchanged inventories, capacity and cleanup PASS.
All backend/audit/secret/Postgres commands pass. Proof:
`../evidence/release-readiness/wp05-cart-details-descendant-clean-20260904.json`.
Next is a distinct0405 Pixel candidate. Signed/device/CI and encompassing Goal
acceptance remain open; this closure does not certify unrelated business flows.

## Provenance and decision

Base `59a8eb6ab1118b3fd17cdbb43d5142a94bceabd5`, canonical worktree
`/Users/walidchraibi/Worktrees/SIT-master-workflow-20260808`, branch
`codex/master-workflow-20260808`. Base was clean; local cached upstream showed
30 ahead / 0 behind. This is not a live remote check. GitHub remains explicitly
deferred: no auth, fetch, remote query, push, workflow or PR action.

The exact base clean R10 completed with exit zero: full technical regression
675 seconds, second Android build44 seconds, byte-identical APKs and all794
entries, unchanged dependency/migration/asset/font inventories, bounded cache
usage and completed cleanup. Backend suite/syntax/audit/secret/Postgres all pass.
Independent execution-only validation passes. Its original machine proof is
`../evidence/release-readiness/wp05-cart-surface-principal-clean-20260904.json`.
That debug proof is NOT a signed successor, new device acceptance or proof of
every descendant flow. Historical installed0404/source55a1aa5a is unchanged.

Read-only follow-up found that the scope protected the direct detail destination
but was not passed into `_ItemDetailsPage` itself. A controlled local probe on
the unchanged base passed4 stable controls and failed4 invariants: selection,
create and overflow popups remained beneath B, and an A custom-list draft could
be saved under a silently replaced B session. An expanded probe additionally
confirmed the gallery survives A→B (stable gallery control passes). Its two
gallery-create rows tapped the obscured underlying heart, not the gallery heart;
those fixture failures are not additional defect evidence. Maintained tests
target the hit-testable gallery heart and assert selector dispatch.

The later intermediate-source disposal probe reproduced a separate same-page
cleanup defect: A's page disposal changed B's previously stored date range into
null. The preceding48 expanded rows passed. Neither defect involved an actual
account, provider, backend write or device mutation. Closing this demonstrated
privacy/data-integrity chain takes precedence over signing another candidate.

## Bounded correction

- Pass the original saved-cart scope into the detail page and onward to its
  wishlist selection/create, overflow, gallery and notices. Do not capture B
  after an await or when the A page is being disposed.
- Bind both wishlist reads/writes to the initiating immutable owner, reject
  stale outcomes, and retain unknown/error versus confirmed-empty semantics.
- Add owned general dialogs using the existing exact route-handle helper.
  Account invalidation and old completion callbacks remove only their A route;
  B's newer page/dialog remains intact. Unscoped shared-widget callers remain
  supported and have positive controls.
- Bind saved-range loading and date/delivery cleanup to that same owner at the
  queued persistence boundary. A disposal cannot capture B; stable A cleanup
  still clears A's own selections. Null-owner legacy calls are not represented
  as comprehensively principal/epoch audited.
- The gallery's existing copy-success message previously had no clipboard
  write. Reuse an actual clipboard write for gallery/menu and emit confirmation
  only after success, under the same current owner. Failure and late replies
  have separate tests; synthetic clipboard handlers prevent real side effects.
  Dispose the gallery's owned page controller.

No reservation, report/support business-flow, payment, public listing, global
auth or provider rewrite is included. Downstream report/reservation flows are
not certified by this saved-state/popup proof. Legal/provider/privacy approval
facts, retention rules, package/version/signing and Store settings stay unchanged.

## Verification checkpoints

Initial corrected nested/detail/general-route combined49 tests pass. Expanded
49 then pass after the separately reproduced disposal defect was corrected.
The expanded service-test helper initially returned Future<void> as Object?;
that compile failure is retained and corrected in the fixture, not waived.
Analyzer first reported3 brace-style infos; no diagnostic was suppressed.

Final focused97 tests and analyzer0 pass. The first full normal gate stopped
at the tool inventory (2229pass/1failed module): the old secondary-details
wiring test assumed the menu began directly with `final choice` and clipboard
logic remained inline. It now requires scope capture/current context before
dispatch and result, follows the shared clipboard/owned-notice helpers, and
rejects six explicit missing-owner/check/await/notice mutations. Existing sheet,
support-branch context, countdown and fallback checks remain. Focused wiring18
passes. No runtime assertion or security requirement was weakened.

Second full normal gate PASS, exit0:2235 tool tests,852 default Flutter tests
with33 declared profile-specific skips and all34 completed Flutter lanes,
analyzer0, Web25s/Wasm dry run, loopback smoke, Android debug22s, minSdk24,
R11 permissions14/exported8 and unchanged source-capacity limits. Working-tree
secret scan and diff check PASS. Exact current committed-source clean R10 is
next and remains OPEN; no focused/normal result substitutes for it.
Machine proof: `../evidence/release-readiness/wp05-cart-details-descendant-ownership-local-20260904.json`.
Hash propagation updates only source digests and derived hash
constants; it asserts every non-hash claim/assertion is unchanged and converges
to a no-op. No new candidate version has been selected or signed.

Next: freeze the corrected source, run exact
clean R10, then prepare a distinct signed Pixel successor and resume bounded
cart/projects acceptance. GitHub, full Goal acceptance and OnePlus remain open.
