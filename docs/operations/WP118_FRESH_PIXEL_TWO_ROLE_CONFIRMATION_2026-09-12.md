# WP118 — Fresh current-candidate Pixel renter/owner confirmation

## Result

The exact signed Internal/Staging candidate `com.shareittoo.app`
`1.0.0+2026091110`, source
`c8e2a49e14f5cae0026fa5f2bc327859fe0ff17b`, passed a fresh physical Pixel
renter/owner core journey on 12 September 2026 local time.

Two distinct previously e-mail-link-verified synthetic principals were used.
The owner published an isolated draft through the app, the Staging server and
public catalog confirmed it, and the renter discovered the exact listing. The
non-binding request/acceptance simulation, renter-visible chat, foreground,
background and terminated-process FCM, owner/renter presentation and
Account-A/B isolation all passed.

This is fresh confirmation of the core two-role journey. WP114 remains the
complete extended current-candidate matrix for listing lifecycle, search and
saved state, cart/projects, media, two-party times, fail-closed location,
privacy export and two consecutive offline/realtime runs. The candidate and
deployed Backend runtime relevant to those results are unchanged.

## Controlled infrastructure retry

The first attempt stopped safely during preparation because the public Staging
TLS connection timed out. It did not reach the device journey. Read-only
diagnosis confirmed the Staging API and database-side container remained
healthy with zero API restarts, while the public proxy route was temporarily
unresponsive from the Mac mini. After both the proxy route and public listings
endpoint returned normally, one controlled retry passed the complete core
journey. No timeout, assertion or product mutation was relaxed.

## Cleanup and boundaries

The exact temporary booking is cancelled, the exact listing is ended, the
fresh public catalog count is zero and the protected owner session is restored.
The empty private artifact directory from the failed attempt was removed; the
successful notification artifact remains owner-only outside Git.

No binding contract, reservation, payment endpoint, real money, Production,
Google Play, tester list, Firebase project, public-registration or PR change
occurred. The MacBook Tailscale peer remains offline, so the physical OnePlus
run is still `NOT_RUN`. It must not be represented as a two-device pass.

The complete local technical regression passes after the evidence update,
including the zero-diagnostic analyzer, Web/Wasm build, loopback smoke,
Android build and the full Flutter, Backend, PostgreSQL, privacy, safety and
release-gate suites. Exact-head GitHub Regression and CodeQL are pending the
evidence commit.

Machine-readable evidence is in
`docs/evidence/release-readiness/wp118-fresh-pixel-two-role-confirmation-20260912.json`.
