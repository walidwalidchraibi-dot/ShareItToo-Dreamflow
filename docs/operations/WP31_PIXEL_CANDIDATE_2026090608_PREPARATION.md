# WP31 Pixel candidate 2026090608 preparation

Status: **PREPARED FOR SOURCE FREEZE**. Exact clean regression, signed archive
and physical Pixel acceptance remain open until this reservation is committed,
pushed and independently verified.

## Why this candidate

WP30 added the missing, principal-owned production UI entry into the secured
V5.2 return-case transaction after the currently installed Pixel candidate was
built. The Pixel and highest verified owner-private archive remain
`1.0.0+2026090607`, source
`0708609f66ecebee75e7d3786130c5231e578148`. That candidate and its evidence
remain immutable rollback truth. A distinct candidate is necessary before any
physical claim can include WP30.

## Provenance and reservation

- Canonical branch: `codex/master-workflow-20260808`.
- Clean synchronized preparation base:
  `9f6e775a93fa6f2e9f1c6f77d4fd6ef007be386f`.
- Reserved identity: `1.0.0+2026090608`.
- Package: `com.shareittoo.app`.
- Highest verified local archive and installed Pixel build before reservation:
  `2026090607`.
- No local `2026090608` archive or checked-in `2026090608` reference existed
  before reservation. This is local uniqueness, not a Play Console claim.

Only the checked-in version and existing V5.2 client-build fallback advance
from `2026090607` to `2026090608`. Any dependent source hashes must be refreshed
mechanically without changing approval, legal, privacy, provider, retention or
live-gate conclusions.

## Required unchanged envelope

- Internal, non-public Staging at `https://staging.shareittoo.com/api/v1`.
- Existing validated Firebase Android Staging input; no Console mutation.
- Canonical ShareItToo upload certificate.
- Google sign-in enabled; Apple and Facebook disabled.
- Closed non-binding `heilbronn_wave0` envelope with technical G3-G5 surfaces.
- Provider hold, no real money, no public registration and no external
  generative-AI runtime.
- Android minSdk 24 and current target/compile SDK; transactional FCM only,
  Analytics off and Crashlytics subject to its separate opt-in.

## Required continuation

1. Freeze and push the preparation source, then verify exact clean identity.
2. Run complete local regression and independent exact-head clean
   reproducibility through the maintained version-2 build profile.
3. Build signed AAB and APK through the normal lifecycle and retain them only
   in the owner-private archive.
4. Independently verify archive contents, hashes, signature, package/version,
   Firebase Staging, privacy surface and provider-hold envelope.
5. Apply only a strictly newer replace update from `2026090607` to
   `2026090608` on the connected unlocked Pixel, then verify installed bytes,
   certificate, version, app-data identity and foreground launch.
6. Bind sanitized closure evidence to the exact candidate source HEAD.

WP31 does not upload to Google Play, alter tester lists or tracks, deploy a
backend, mutate Firebase Console, invoke payment/KYC, enable Production or
public registration, change Cloud/VPS/DNS, contact OnePlus or merge PR #7. The
physical V5.2 return-case product journey is a later separate package.
