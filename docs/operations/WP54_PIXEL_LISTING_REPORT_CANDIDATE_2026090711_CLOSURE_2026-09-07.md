# WP54 — Pixel listing-report candidate 2026090711 closure

Status: **COMPLETE ON THE PHYSICAL PIXEL, LOCALLY AND ON GITHUB**.

## Frozen candidate

- Worktree: `/Users/walidchraibi/Worktrees/SIT-master-workflow-20260808`.
- Branch: `codex/master-workflow-20260808`.
- Candidate source HEAD: `c819c5f4445d0b998b2ee319e64f8c95cfb01eda`.
- Identity: `com.shareittoo.app`, `1.0.0+2026090711`, minSdk 24,
  targetSdk 36, Internal channel.
- API: `https://staging.shareittoo.com/api/v1`.
- AAB SHA-256:
  `a2e72a5afd09e44a20c82d60236e63143d963f7a3ba6ac6e95389e87ce1e3681`.
- APK SHA-256:
  `eb74c3978a347412d0cc104973ff8c3f90cb796334d54ea26c3dcc91ef943407`.
- Upload certificate SHA-256:
  `098f485e57161558e911fc3c742845925584db31c474cdba08dda02feb0129a4`.

The owner-only four-file archive passes the maintained current-head validator.
Its binary privacy scan passes, Firebase is configured, Google authentication
is enabled, Apple and Facebook are disabled, and the closed non-binding
`heilbronn_wave0` envelope remains explicit. No older candidate was overwritten.

## Build and installation proof

All 2,448 repository tool tests and the complete profiled local regression pass
through Flutter, Backend, PostgreSQL, analyzer, Web/Wasm, loopback smoke,
Android minSdk 24 and the release build path. The signed APK and AAB were built
once through the maintained Internal Staging profile without a timing, cache,
rate-limit or parallelism workaround.

During evidence authoring, one unchanged parallel tool-suite invocation ended
incompletely with 2,440 of 2,442 registered tests passing and two failures. Its
truncated console stream did not preserve a defensible failure classification.
No source, runner option, concurrency or timeout was changed to obtain green.
The same standard command then passed once and a deliberate three-run
stability proof passed 2,448 of 2,448 tests on every run. The anomalous
invocation is disclosed rather than promoted to evidence; no workaround or
release prerequisite was created.

The Pixel 7 Pro was replace-updated directly from `1.0.0+2026090610` to
`1.0.0+2026090711`. Installed bytes and certificate match the private archive;
the original installation time and application-data inode were preserved. No
uninstall, downgrade, data reset, account deletion or OnePlus contact occurred.

## Exact physical listing-support proof

An owner-only source vault supplied two existing, separately email-verified
synthetic Staging principals. Its bytes and mode remained unchanged. An
isolated copy created exactly one temporary active listing without a booking,
reservation, contract, payment or monetary effect.

On the physical Pixel, the renter role found that exact listing, opened its
three-dot menu and selected `Anzeige melden`. The real support intake required
the immediate-danger answer and single-issue confirmation, retained the listing
context through the technical-problem route and returned a canonical
server-confirmed receipt. Independent authenticated readback then proved
exactly one recent support case with:

- the exact temporary listing as `linkedListingId`;
- `caseType=general_help` and `caseSubType=app_error_or_display`;
- `operatingMode=simulation` and `status=received`.

No success was inferred from navigation or local state. The synthetic support
case remains as the intended auditable Staging intake record. The temporary
listing was subsequently ended and is absent from the public catalog, the
isolated fixture is marked retired, and the protected owner session was
restored after a normal app restart. The restart was needed only because the
first post-logout UI automation did not reach its expected surface; it was not
a build, test or release prerequisite and did not alter data semantics.

## Exact GitHub closure

- Regression `34157382861`: success at the candidate source HEAD, including
  independent clean-checkout reproducibility.
- CodeQL `34157382862`: success at the same HEAD.
- Open code-scanning alerts: zero.
- PR #7: Draft, open, mergeable and unmerged.
- Candidate source remote divergence before this evidence commit: `0/0`.

## Boundaries and remaining holds

WP54 performed no Google Play upload or track/tester change, no Backend or
Firebase Console deployment, no Production/public-registration action, no
payment/provider/Cloud/VPS/DNS change and no PR merge. Listing AI external
execution, Apple/Facebook authentication, real money, professional V5.2 legal
approval and authenticated VPS parity observation retain their existing holds.
