# WP72 — current-candidate report and block lifecycle

Status: **COMPLETE ON PIXEL, LOCALLY AND ON GITHUB**.

## Exact scope

WP72 closes the remaining physical report/block gap on the unchanged signed
Internal Staging candidate `com.shareittoo.app` `1.0.0+2026090904`, source
`12b88cf97f91973d6dfd59fe3f4dcb9c915dc7d0`. The installed Pixel APK,
private archive and canonical upload certificate match. No mobile or Backend
runtime path changed after the candidate source.

## Physical result

On the Pixel 7 Pro, a fresh isolated Staging listing completed the real app UI
path from exact listing options through `Anzeige melden`, reason
`Betrug / Täuschung` and `Meldung gesendet`. Independent Staging readback
proved exactly one accepted report for the target after a zero-count preflight.
The audit report remains intentionally retained.

The same exact owner was then blocked from the public profile. Independent
readback proved exactly one block; both the target and another listing from the
same owner disappeared from Explore. `Blockierte Nutzer` displayed the exact
entry. Physical unblock produced the empty server-confirmed block truth, and
the target listing became visible again.

A separate non-binding zero-money thread proved message-list isolation. Its
chat was visible before blocking and absent from `Alle` while the owner block
was active; the standard server thread list also excluded it. No send action
was available or attempted during the block. Before that proof, the UI
correctly rejected blocking while an accepted upcoming simulation still
existed and required cancellation first. Cancellation was independently
confirmed with no contract, payment, reservation or monetary effect.

## Cleanup and evidence boundary

All three isolated listings, including one older same-owner diagnostic listing,
are ended and absent from the public catalog. The non-binding booking is
cancelled, blocking is empty, all diagnostic sessions are revoked and the
protected owner session is restored. The moderation report remains as audit
history. No account identity, fixture identifier, credential, token, raw
device identifier or private path is stored in repository evidence.

The durable interaction helper selects the unique smallest exact-label Flutter
semantics region for the intentional 1.2-second stationary long press and
requires an owner-bound confirmation. It fails closed for missing, disabled,
ambiguous or malformed targets. Message-list truth requires exact content and
server readback; the persistent bottom-navigation label is never accepted as a
loaded destination.

No Production, app or Backend runtime, deployment, Google Play, tester,
Firebase, external identity/payment provider, money, Cloud/VPS/DNS, OnePlus or
PR-merge state changed.

## Verification closure

Forty-two focused tests and all 2,531 tool tests pass. The complete local
technical regression passes at exact implementation HEAD
`104ee9c166d3e56e0edebc3db7d638d6017ecd66`, including Flutter analyzer, the
full Flutter and targeted security/product suites, Web/Wasm, loopback smoke and
the Android debug build. Exact-head GitHub Regression `34363123880` and CodeQL
`34363123877` pass, and the current PR merge ref has zero open Code Scanning
alerts. PR #7 remains Draft, open, mergeable and unmerged.

Machine-readable evidence:
`docs/evidence/release-readiness/wp72-current-candidate-report-block-20260909.json`.
