# WP107 — Current-Candidate Extended Pixel Two-Role Test

## Result

The exact signed Internal/Staging candidate `com.shareittoo.app`
`1.0.0+2026091101`, source
`67c4e2ebe4ceead7529c4f02ca1209d71c66fe25`, completes the full currently
executable payment-free renter/owner scope on the physical Pixel 7 Pro. This is
not a claim that binding contracts, payments, returns, damage cases or reviews
have passed: the Staging server correctly stops binding acceptance with
`409 v52_contract_documents_unavailable` until professionally approved V5.2
document snapshots exist.

## Physical scope passed

- Owner draft edit, publish, pause, reactivate and end passed through the app
  with independent server and public-catalog readback.
- Renter exact search, category filtering, listing detail, save, cold restart,
  Account-A/B isolation, restoration and removal passed.
- Rental-cart idempotency, non-reserving behavior, project creation,
  assignment, restart persistence, account isolation and baseline restoration
  passed.
- Private synthetic image attachment, counterparty visibility, two-party
  handover time, two-party return time and terminated-process persistence
  passed. Exact location before its server reveal window was rejected and no
  location message was created.
- A fresh email-verified non-binding conversation passed a real Wi-Fi-off
  interval: the controlled message remained absent offline, appeared after
  network restoration in the same surviving app process, produced no fatal
  entry and left the original network state restored.

The first search/saved run stopped safely because the exact renter-restored
surface was not observed. Cleanup, empty catalog and protected owner restoration
were independently confirmed. An isolated owner-renter-owner binding smoke then
passed; the unchanged driver subsequently passed. No assertion or timeout was
weakened, so this is retained as a bounded observation gap rather than a
permanent workaround.

## Durable diagnostic correction

The older offline/realtime driver required a binding accepted booking and was
therefore unusable behind the intentional V5.2 legal-document gate. Its fixture
selector now also accepts only an exact server-confirmed non-binding simulation
that proves availability remained unaffected and payment access was rejected.
A dedicated wrapper prepares and retires that fresh simulation while proving
the protected source vault unchanged. The binding validation remains intact,
and focused tests fail closed for unsafe simulations.

## Safety and cleanup

Every temporary booking was cancelled, every temporary listing was ended and
removed from the public catalog, the device media file was removed, the
protected owner session was restored and the public Staging catalog read back
empty. No contract, reservation, payment endpoint, money, Production, Store,
tester list, Firebase project, VPS/cloud/DNS, public registration, OnePlus or PR
merge changed.

The physical report/block/unblock proof remains historical from WP72. Its
relevant UI, moderation, blocking and message-isolation implementation is
unchanged in the current artifact; it was not physically replayed in WP107 and
is not mislabeled as a new current-candidate device result.

## Remaining gates

After professional V5.2 legal snapshots and owner approval, the binding
acceptance, pickup/return confirmation, damage/needsReview and review sequence
must be replayed. Official Stripe sandbox ownership must separately be ready
before test payment, refund and simulated payout. The exact OnePlus replay stays
open until the remote MacBook device connection returns.

Machine-readable evidence:
`docs/evidence/release-readiness/wp107-current-candidate-extended-pixel-two-role-20260911.json`.

## Closure verification

Implementation and evidence commit
`79f7985f85ce6826c89f10dc7bab5cf905118913` passes the full local technical
regression and exact-head GitHub Regression `34555758947`, including the R10
clean-checkout reproduction, Flutter, backend and PostgreSQL jobs. Exact-head
CodeQL `34555758981` passes and the current branch has zero open code-scanning
alerts. PR #7 remains draft, open, mergeable and unmerged.
