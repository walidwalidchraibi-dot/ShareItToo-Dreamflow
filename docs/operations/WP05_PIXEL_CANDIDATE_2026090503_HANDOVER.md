# WP05 — full-surface Pixel candidate 2026090503 closure

Status: the exact signed Staging/internal candidate, its physical Pixel
acceptance, the two-role non-binding product journey and controlled FCM matrix
pass. Binding legal, Stripe, external listing-AI, Store, OnePlus, production and
public-release gates remain separate. The encompassing product Goal remains
ACTIVE.

## Exact candidate and provenance

Candidate `1.0.0+2026090503` is frozen at
`96b97b55983111d9e0ae8d8fcc91e9e241a2cb6f` on
`codex/master-workflow-20260808`. It is package `com.shareittoo.app`, minSdk 24,
target/compileSdk 36, internal channel and Staging API. Firebase is configured;
Google sign-in is enabled while Apple and Facebook remain disabled. The closed
pilot, acknowledged Stage-A non-binding pilot, Blue Ocean listing assistant and
the G3/G4/G5 technical surfaces are compiled in. This is not a Play or public
release.

The independently verified AAB SHA-256 is
`3a005e49f942d31d3284ba708b2d447c5141670b3c9da09ca6909d33794c2204`;
the APK SHA-256 is
`ff9f0527c73cc7ba7abf31c1fa478c061f292e7b7cd485500959dfe12205ef57`;
the compiled privacy report SHA-256 is
`bd3bb5ba143da2dd6e7cf319b9c1977898ceb04f3172c968db2d0963437be6fb`;
and the upload certificate SHA-256 is
`098f485e57161558e911fc3c742845925584db31c474cdba08dda02feb0129a4`.
Archive, APK/AAB signatures, bundle structure and compiled privacy surface pass
independent verification.

Two rejected predecessors remain separate evidence. Candidate 2026090501 at
`ec016ae4dcafa2c27cee03ce753bb1def89f489f` exposed a transient startup theme
hydration race and was superseded. Candidate 2026090502 at
`60d34e85c6e777d1e755d9aec8619d3a75c95736` used incorrect build-variable names,
therefore omitted the required pilot/G3/G4/G5 feature envelope, and was never
installed. Neither predecessor is promoted by this closure.

## Local, reproducibility and physical Pixel proof

The exact 0503 source passes the complete local technical regression, analyzer
with zero issues, Web/Wasm, loopback, Android build and signed artifact
lifecycle. Exact clean R10 rebuilt from detached source, produced a
byte-identical APK on the second Android build, retained clean source and
artifact inventories and completed cleanup.

The Pixel received a data-preserving update: installed package, version, APK
bytes and signing certificate match the frozen archive, while the original app
installation/data identity was retained. Authenticated cold start and the
candidate-generic surface matrix pass. Private visual review confirms readable
main navigation and background selection surfaces. An explicit dark background
correctly remains dark across a system-theme toggle; this is intentional
persisted user choice, not the earlier system-light/dark mismatch.

## Two-role product journey and controlled push

A fresh, combined physical journey passes with two distinct previously
email-link-verified Staging principals. It publishes the owner's draft through
the Pixel UI, verifies the listing as active on the server and in public renter
discovery, creates and accepts only the authorized non-binding simulation,
checks owner and renter truth surfaces, chat visibility and Account A/B
isolation, and then cancels the simulation, ends the listing and restores the
protected owner session.

During that same bounded journey, controlled FCM passes in foreground,
background and terminated-process states. The notification uses a generic,
privacy-preserving title/body and the ShareItToo icon. Its private screenshot
also contains unrelated device notifications, so it remains outside Git and is
not distributable; sanitized evidence records only SHA-256
`e466e064b4bd4707fe9fbca465377a706b3bb80ffdabf42cf99c4ad6a22f3d79`.

The maintained diagnostic tooling was then corrected so controlled FCM may use
the exact payment-free non-binding simulation rather than depending on binding
legal documents. It also no longer reuses an explicitly paused/ended synthetic
listing with the same title. Focused tests and the complete regression pass.
The tooling/evidence implementation is committed separately at
`d2af1d842b56dd75a86f892f128c469084faeb65`; its hash-only ratchet propagation
changes no approvals, provider states or legal assertions.

## GitHub proof

At the immutable candidate source, Regression run
`https://github.com/walidwalidchraibi-dot/ShareItToo-Dreamflow/actions/runs/33935962863`
and CodeQL run
`https://github.com/walidwalidchraibi-dot/ShareItToo-Dreamflow/actions/runs/33935962806`
pass. At the separate tooling closure, CodeQL run
`https://github.com/walidwalidchraibi-dot/ShareItToo-Dreamflow/actions/runs/33939051309`
passes; exact Regression run
`https://github.com/walidwalidchraibi-dot/ShareItToo-Dreamflow/actions/runs/33939051291`
passes, including its exact clean-checkout reproducibility job. PR #7 remains
Draft, open, mergeable and unmerged.

## Retained boundaries and next work

A controlled attempt to prepare a binding fixture correctly stopped at
`409 v52_contract_documents_unavailable`, because the V5.2 legal snapshots are
still draft-blocked. No binding booking, contract, reservation or payment was
created; the newly prepared unbooked synthetic listing was paused and verified
non-public. This legal gate was not bypassed.

No Google Play, OnePlus, production, public registration, live-money, SMS, KYC,
Firebase configuration, Cloud/VPS/DNS, tester-list or PR-merge action occurred.
The next highest-value work is to verify the now owner-prepared provider access
read-only, then close the Google sign-in/listing-AI and Stripe sandbox/test-money
Staging matrices as separate packages. Binding V5.2, Store, production and live
money remain closed until their exact owner/legal gates are satisfied.

Sanitized machine evidence:
`docs/evidence/release-readiness/wp05-pixel-0503-closure-20260905.json`.
