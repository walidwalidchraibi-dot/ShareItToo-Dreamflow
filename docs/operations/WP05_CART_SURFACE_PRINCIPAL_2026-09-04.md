# WP05 saved-cart surface — consolidated principal ownership

Status: consolidated correction IN PROGRESS; package closure still OPEN.

## Latest consolidated-source checkpoint

Nested saved-folder ItemCard heart, context menu, list selection and nested
creation now carry the original folder scope. Every A-owned popup/detail page
is removed by its exact route identity; foreign B pages/dialogs survive. The
nested draft owns and disposes its text controller. Scoped local writes retain
the initiating principal; silent replacement clears the old root snapshot.
Unrelated unscoped shared-widget callers retain their behavior, with three
positive add/create/remove controls. All five wishlist options remain present;
the scoped wishlist menu uses the standard owned popup. No actual share action
or provider interaction is performed by these tests.

Actual cart-origin tests cover both guest login entrances, all three enabled
technical destinations, silent account replacement before click, checkout
recheck/catalog responses after A-to-B, missing cart item without stale fallback,
late error, exact destination removal and A-finally versus a newer B busy
checkout. Destination reads use synthetic intercepted replies. These tests
certify the origin and its route lifetime, NOT downstream planner/booking/set,
reservation, contract, payment or every nested detail-screen business workflow.

Focused default78 (includes nested24 and guest2), enabled navigation14,
wiring20, full analyzer0 and secret scan PASS. Enabled HTTP40+assignment7+
project8 and navigation14 also pass again in the complete current normal gate.
Exact hash propagation changes only source hashes and already-exact dependent
ratchets; a repeat makes zero changes. No legal/privacy/retention/provider or
approval fact changes. The separate G2 lifecycle validator change follows the
extracted helper chain instead of requiring the direct call in itemDetails;
seven mutation probes reject a missing call, owner, current-owner check, notice
or truthful status. Existing lifecycle/retention tests remain mandatory.
Full normal PASSES:2232 tool tests,812 default Flutter tests plus all mandatory
profiles, analyzer0, Web25s/Wasm dryrun, loopback, Android44s, minSdk24/R11.
Capacity guard passes without extra cleanup. Exact clean-source and
successor/device proof remain OPEN; the next step is a distinct source commit
and unchanged R10 from that exact clean commit. No signing yet.

Final bounded hypothesis check: six private deterministic page/draft/notice
probes schedule scope invalidation at the return boundary of the real async
current-owner check, with stable controls and a foreign destination. All six
pass. The suspected additional route gap did NOT reproduce and prompted no
runtime change. Do not describe that hypothesis as another confirmed defect.

Retained current-turn failures and their classification:

- Initial nested probe: four stable controls pass, three specific popup-leak
  assertions fail; a fourth heart assertion used the generic root label
  `Gemerkt` and is ambiguous, NOT independent leak evidence. The maintained
  heart assertion targets `Aus Gemerkt entfernen` and passes after propagation.
- Expanded fixture initially used `endsWith('move')`, also matching `remove`,
  compared the entire envelope despite legitimate B default-list initialization,
  and left the documented two-second notice timer pending. Corrected tests tap
  the intended action, compare every pre-existing principal partition unchanged,
  assert B has no A assignment/custom list, and advance only the specified notice
  lifetime after behavioral assertions. No readiness sleeps or retries added.
- Two navigation compilations omitted required synthetic item fields; fixed.
  Context/brace analyzer findings were fixed without suppressions. Generic
  `<void>` and stronger lifecycle-helper wiring replaced obsolete literal matchers.
- First full normal:2229/2231 tool checks pass; two failures share the old G2
  direct-call expectation. The new helper-chain check and seven mutation probes
  pass (G2 suite9). This failed full run remains failed, not waived or relabeled.

Current source/binding/log hashes and results are recorded in
`docs/evidence/release-readiness/wp05-cart-surface-principal-consolidated-20260904.json`.
All logs remain private; old intermediate proof remains separately source-bound
in `wp05-cart-surface-principal-progress-20260904.json`. Installed0404/source55a1
and b28 assignment clean proof are unchanged. No GitHub, signed build, device,
real-account/backend/provider/Store/production or payment action.

## Previous intermediate-source focused checkpoint

The red-source and assignment clean-proof documents were committed separately
as `af20cb4df174370982862909c41dd144450b851b` (no push). The owned working-tree
implementation now pins four direct cart actions, their prerequisite catalog
reads, root results/notices, wishlist creation/rename/delete/inline removal and
folder/menu lifetime. A shared saved-cart scope owns exact routes and detects
silent session replacement; the listing-to-cart helper reuses it. Unrelated
general auth/catalog/wishlist callers keep their previous contracts.

Current focused results:61 default/widget/local tests,55 enabled intercepted
HTTP tests,18 wiring guards, analyzer0. These include earlier project and
assignment controls; they are not full regression or device acceptance. The
three technical destination origins and nested shared ItemCard heart/context
menu/selection/details routes still require explicit caller coverage before
this package can close. Source-inventory hash propagation, complete normal and
exact clean regression are NOT done for this working tree. Do not freeze a
successor or treat these findings as a new completed Goal.

Additional intermediate-source truth probes (not the historical b28 source)
failed13 invariants: absent/non-list/invalid cart collections could become
empty success and an unrelated nonempty cart could acknowledge an add. Owned
responses now validate the required collections and identities; add requires
the exact generated item ID, listing, dates and project. The original ownership
probe's stable add stub only established response handling; its maintained
positive fixture now echoes the exact request identity. Negative malformed and
unrelated-cart fixtures remain mandatory and pass. No claim of real persistence.

Retained intermediate failures: two silent-switch folder checks exposed the
parent's stale A mosaic after the owned folder closed; mismatch detection now
emits an account-state refresh signal without changing credentials. Five
overlay tests did not establish dispatch when calling outside a settled user
interaction; an isolated control reproduced that fixture failure. Actual button
interaction now establishes dispatch and all five pass. A route-helper test
initially had a generic-type compilation error. Its corrected idle-dispose row
then waited for an unscheduled cleanup frame; the run was deliberately
interrupted after three rows, NOT passed despite the wrapper's zero exit code.
The scope now requests its own visual update; all13 route tests pass, including
an assertion that production code schedules the frame and B remains current.
No sleeps, timeouts, concurrency changes, skipped assertions or forced test-only
frames substitute for that behavior. New wiring checks initially matched a
comment and then missed formatter whitespace; the comment/whitespace matcher
were corrected, keeping the exact forbidden calls and required refresh target.
Lint findings were fixed without suppression. All earlier logs remain retained.

The in-progress machine proof records current source/log digests separately:
`docs/evidence/release-readiness/wp05-cart-surface-principal-progress-20260904.json`.

## Historical red-source decision

Tested source: `b28da2aa06816fd47eefe3a6657e7674e92cce03` in canonical
`/Users/walidchraibi/Worktrees/SIT-master-workflow-20260808`, branch
`codex/master-workflow-20260808`. Only evidence documents changed during probes.
Owner explicitly deferred GitHub: no auth, remote read, CI, PR or push action.
The preceding assignment correction has its own exact clean proof; these new
findings do not relabel that proof as full-cart acceptance. No new signed build,
Pixel, OnePlus, real account, provider or backend mutation occurred.

## Why this package is next

The remaining paths on the same cart surface still capture a global session
after asynchronous work or install results using only `mounted`. Controlled
probes now demonstrate the failure class beyond creation/assignment. Closing
these privacy/data-integrity paths takes priority over new device mutation or
non-blocking support work. Bundle the remaining cart-surface corrections before
another signed candidate; this is not a new feature or broad unrelated hardening.

## Observed versus still inferred

| Path | Current proof | Consequence and scope |
| --- | --- | --- |
| Add cart item | Stable mocked response passes; late A200 is accepted after B; A401 triggers a refresh using synthetic B credentials | Pin dispatch, refresh policy and result to A; actual server persistence is not claimed |
| Delete cart item | Same service failures plus late A UI snapshot replaces an independently loaded B snapshot | Reject stale result; preserve B state and foreign route |
| Delete project | Same service and UI failures | Same owner chain; no real project was deleted |
| Recheck cart | Same service and UI failures | A availability/price response must not become B state |
| Create custom wishlist | Stable A passes; A dialog remains beneath B; confirming A draft after silent B replacement creates its name under B | Capture owner before dialog, close the exact A route, bind local write |
| Cart-item/booking-group/planner/listing-set navigation | Static risk: session/read awaits with mounted-only navigation and late cleanup | Add targeted controls before claiming safe navigation; no dynamic navigation leak is claimed yet |
| Wishlist folder, rename/delete and inline folder actions | Static risk: folder does not bind its lifetime to the principal; dialogs use global completion/current principal | Classify with exact-route/owner tests; do not claim these rows already passed or failed dynamically |
| Item-details cart entry, two call sites | Static risk: `_storeRentalCartIntent` has no owner and its notice is untracked | Cover the cart action only, not unrelated reservation/edit business flows |

Enabled HTTP/service/UI probe:7 stable controls PASS /11 desired invariants FAIL.
This is four service controls, four late-success failures, four foreign-refresh
failures, three stable UI controls and three stale-UI failures. The B UI snapshot
is independently observed before completing A's response; B's foreign dialog
survives, but A's private project text incorrectly reappears beneath it.
Separate default wishlist probe:1 stable control PASS /2 desired invariants FAIL.
Total:8 controls pass and13 invariant failures across isolated synthetic tests.

These are local HTTP interception and preference/widget fixtures. Fabricated
example.invalid identities and fake credentials are used, never real account
data. Mock success controls establish bounded response handling, not real
deletion, payment, contract or provider acceptance. In the401 probe the simulated
B refresh returns503; a successful B refresh/retry or real backend write is not
claimed. The prohibited cross-principal refresh attempt itself is observed.

The first combined run had6 passes/12 failures. Its two delete-project widget
rows failed because direct callback invocation did not establish request start;
those two rows are NOT product-defect proof. The fixture now taps the actual
localized chip delete button and verifies request start. Final corresponding
stable control passes and the late-A failure reproduces as above. No timeout,
sleep, test parallelism, runtime code or result assertion was relaxed. Preserve
both logs; fixture debt is closed by the real interaction, not by a waiver.

## Bounded implementation sequence

1. Inventory all existing cart and saved-folder action entry points and record
   their initiating owner, awaited work, dispatch, result, notices and navigation.
   Retain the already-fixed project creation/assignment/read cases as controls.
2. Reuse the existing credential-free principal/epoch type for the four direct
   cart mutations and their guest-sync/local-queue prerequisites. Add owner-bound
   repository methods where missing; never globally refresh as a new account.
3. Bind direct cart results and notices, custom-list creation and folder/dialog
   lifetime to their initiating owner. Track exact routes; A cleanup must never
   pop B's current navigator or clear B's newer handle. Cover rename/delete and
   inline folder actions as part of this surface, after reproducing the relevant
   risk. Pass explicit owners at covered local-write boundaries; preserve
   unrelated persistence behavior unless a stronger contract is explicitly tested.
4. Guard cart-origin navigation before each awaited read, remote call, route
   opening and post-route work. Bind the lifetime of any A-owned destination
   by exact route identity while preserving B destinations. Cover only the
   item-details cart helper, not reservation/edit/payment business flows.
5. Make safe current-account/guest behavior and interrupted/silent/epoch-change,
   late200/401/error, malformed session, foreign dialog/page and old-finally/new
   handle behavior permanent tests. Failed or uncertain operations must not
   become successful saves, false empty state or definite non-mutation claims.
6. Refresh only exact source hashes and dependent constants; retain every legal,
   privacy, provider and approval fact. Run focused tests, analyzer, full normal
   regression and exact clean proof on the consolidated final source.
7. Only then select a distinct successor version/signing envelope and resume
   real Pixel cart/core acceptance. Never rebuild into0404 archives or promote
   previous-source device evidence. GitHub remains deferred, not waived.

## Exit, limits and stop conditions

Closure requires all listed entries classified, confirmed defects corrected,
their negative controls green without weakening stable behavior, exact final
source proof and an honest remaining-device/provider matrix. No schema, pricing,
contract, reservation, refund, provider-hold, public registration, Store or
production changes are included. Broader real AI, Stripe sandbox, legal/operator
facts, full Pixel functionality and later OnePlus acceptance remain OPEN.
If an unrelated business/security issue is discovered, record its evidence and
priority rather than silently widening this package. Stop the affected action
on contradictory principal, release or protected-data evidence; continue other
independent in-scope work. Do not request another owner gate for these already
authorized local corrections, and do not begin another signed candidate while
the confirmed P1 cases remain open.

Machine proof: `docs/evidence/release-readiness/wp05-cart-surface-principal-red-20260904.json`.
Private probe sources/logs remain in the current task workspace. Their digests
are recorded in that proof; no raw private runtime/device evidence is committed.
