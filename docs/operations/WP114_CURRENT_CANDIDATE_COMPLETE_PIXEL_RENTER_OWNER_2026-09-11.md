# WP114 — Current-Candidate Complete Pixel Renter/Owner Matrix

## Result

The exact signed Internal/Staging candidate `com.shareittoo.app`
`1.0.0+2026091110`, source
`c8e2a49e14f5cae0026fa5f2bc327859fe0ff17b`, passes the complete currently
executable non-binding renter/owner matrix on the physical Pixel 7 Pro against
immutable Staging Backend head
`df39a14b7a19afe467842461a28f1e77fec8445e`.

This is a real device and server proof, not a binding contract or payment
claim. Professional V5.2 snapshots are still unavailable and the payment
provider remains the zero-money memory simulation. Therefore pickup/return
completion, damage/needsReview, review, Stripe test payment/refund and simulated
payout stay separate closed gates.

## Passed renter/owner journeys

- Two distinct email-link-verified synthetic principals passed owner/renter
  switching, Account-A/B isolation and protected-owner restoration.
- The owner created and published a draft through the Pixel UI; Staging and the
  public catalog independently confirmed it. The renter discovered the exact
  listing and opened its details.
- A non-binding request was accepted, and its chat was visible to the renter.
  No contract, reservation, payment endpoint or monetary effect occurred.
- FCM passed in foreground, background and terminated-process states. The
  hash-bound private notification-shade capture shows clear, correctly branded
  ShareItToo icons without clipping, blank substitution or placeholder art.
- Listing edit, publish, pause, stable public hide, reactivation and terminal
  end passed with strictly advancing server revisions.
- Exact and category search, detail, save/remove, restart persistence and
  Account-A/B saved-state isolation passed.
- Rental-cart submission was idempotent and non-reserving. Project creation,
  assignment, restart persistence, account isolation and exact baseline restore
  passed.
- Synthetic private image attachment, counterparty visibility, two-party
  handover time, two-party return time and terminated-process message
  persistence passed. Exact location remained blocked before the server reveal
  window, and no premature location message was created.
- The real password-confirmed privacy export passed exact-principal isolation
  and credential absence; raw content and its temporary no-network receiver
  were removed.
- Two consecutive fresh offline/realtime runs passed without relaxed timing or
  mutation retry. During each 15-second offline interval the new message stayed
  absent; the same surviving process received it after restoration, with zero
  fatal entries and exact network restoration.

## Cleanup

Every isolated listing is ended and publicly absent. Every temporary booking
is cancelled, all temporary media and privacy receiver data are removed, all
temporary journey vaults are retired, and the protected owner is restored. A
fresh public readback reports zero synthetic journey listings and zero public
listings overall. No unrelated listing was inspected or changed.

The first offline attempt stopped safely before detailed child diagnostics were
preserved. Cleanup and network restoration were independently confirmed. The
retained runner now exposes only a bounded non-sensitive child reason; two
subsequent fresh runs passed identically. No timeout increase, retrying app
mutation, reduced assertion or network workaround was introduced. The initial
negative attempt remains recorded in the technical observation log.

## Verification and remaining gates

The deployed Backend head passed the complete local regression, GitHub
Regression `34627911500` including independent clean checkout and API-image
publication, and CodeQL `34627908397`. The separate diagnostic implementation
head `1d053ff28122aefb3e3b0dd618e356c870ebfb7f` owns the guard corrections;
its final local/GitHub conclusions are bound in the machine-readable evidence.

No Production, Google Play, tester list, Firebase project, public registration,
OnePlus, real money or PR merge changed. Exact-current-candidate Listing-AI,
professional V5.2 binding, Stripe sandbox and the separate OnePlus/two-device
replay remain open. Machine-readable evidence is in
`docs/evidence/release-readiness/wp114-current-candidate-complete-pixel-renter-owner-20260911.json`.
